import "server-only";
import type { Database } from "@/lib/supabase/database.types";
import { createServerSupabase } from "@/lib/supabase/server";
type Tables=Database["public"]["Tables"];
export type CloudWorkspaceDTO={workspace:Tables["workspaces"]["Row"];role:Tables["memberships"]["Row"]["role"];clients:Tables["clients"]["Row"][];archivedClients:Tables["clients"]["Row"][];inquiries:Tables["inquiries"]["Row"][];archivedInquiries:Tables["inquiries"]["Row"][];activity:Tables["audit_events"]["Row"][]};
export async function readCloudWorkspace(userId:string):Promise<CloudWorkspaceDTO|null>{const supabase=await createServerSupabase();const{data:membership,error}=await supabase.from("memberships").select("workspace_id,role").eq("user_id",userId).limit(1).maybeSingle();if(error||!membership)return null;const workspaceId=membership.workspace_id;const[workspace,clients,archivedClients,inquiries,archivedInquiries,activity]=await Promise.all([supabase.from("workspaces").select("*").eq("id",workspaceId).single(),supabase.from("clients").select("*").eq("workspace_id",workspaceId).is("archived_at",null).order("name").limit(100),supabase.from("clients").select("*").eq("workspace_id",workspaceId).not("archived_at","is",null).order("updated_at",{ascending:false}).limit(100),supabase.from("inquiries").select("*").eq("workspace_id",workspaceId).is("archived_at",null).order("updated_at",{ascending:false}).limit(100),supabase.from("inquiries").select("*").eq("workspace_id",workspaceId).not("archived_at","is",null).order("updated_at",{ascending:false}).limit(100),supabase.from("audit_events").select("*").eq("workspace_id",workspaceId).order("created_at",{ascending:false}).limit(25)]);if(workspace.error||!workspace.data)throw new Error("Не удалось загрузить рабочее пространство");return{workspace:workspace.data,role:membership.role,clients:clients.data??[],archivedClients:archivedClients.data??[],inquiries:inquiries.data??[],archivedInquiries:archivedInquiries.data??[],activity:activity.data??[]}}
